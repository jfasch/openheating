* remove ThermometerCenterBase.get_thermometer(), in favor of
  ThermometerCenterThermometer

* remove SwitchCenterBase.get_switch(), in favor of SwitchCenterSwitch

* logger.exception() in all object_*.py classes, where exceptions are
  converted

* split thinking.py into multiple dedicated files

* figure out how to properly (centrally) define dbus interfaces. will
  have multiple Brain implementations around someday.

* JFControl: move out into installations/

* add tests for switchcenterclient, thermometercenterclient

* investigate on method resolution, calling. intercepting method
  calls. cure the broken openheating/dbus/service_config_native.py.
